// DO NOT USE THIS FILE.  It is not finished yet.

var error;

initInstall("JiveAudio", "@linuxfront.com/JiveAudio,version=2.0", "2.0.5.0");

var pluginsFolder = getFolder("Plugins");

// @dlname@

error = getLastError();
if (error != 0)
  cancelInstall(error);
else {
  performInstall();
}
